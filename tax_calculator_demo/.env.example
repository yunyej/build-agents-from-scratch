# Optional: use the shared book key in code/.env instead of copying this file.
# If you use this file, copy to .env in this folder (gitignored).
OPENAI_API_KEY=

# Optional overrides (defaults are fine for the demo):
# OPENAI_CHAT_MODEL=gpt-4o-mini
# RAG_ROOT=../rag_federal_individual
# TRACE_DB_PATH=data/traces.sqlite
